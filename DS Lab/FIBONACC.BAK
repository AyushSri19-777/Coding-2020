#include <iostream.h>
#include <conio.h>
int main()
{
  int a=0,b=1,c=0,i,n;
  cout<<"Enter the limit\n";
  cin>>n;
  cout<<a<<","<<b<<",";
   for(i=3;i<=n;i++)
     {
       c=a+b;
       if(i<n)
       cout<<c<<",";
       else
       cout<<c;
       a=b;
       b=c;
     }
    getch();
  return 0;
}